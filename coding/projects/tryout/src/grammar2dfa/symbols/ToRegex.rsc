module grammar2dfa::symbols::ToRegex

import Grammar;
import grammar2dfa::symbols::SymbolToString;

/* yet to be implemented properly 
 * TODO: make this
 */
str symbolToRegex(Symbol s) {
	return symbolToString(s);
}